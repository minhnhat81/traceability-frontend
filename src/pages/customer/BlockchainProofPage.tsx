import { Typography } from "antd";
const { Title, Paragraph } = Typography;

export default function BlockchainProofPage() {
  return (
    <div>
      <Title level={3}>Blockchain Proof</Title>
      <Paragraph>Tra cứu bằng chứng on-chain (Polygon, IPFS,...).</Paragraph>
    </div>
  );
}
